apiVersion: v1
kind: Service

metadata:
  name: node-service
  namespace: jenkins-demo

spec:
  selector:
    app: node-app

  type: NodePort

  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30080